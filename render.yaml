services:
  - type: web
    name: superme-bot
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: TOKEN
        scope: run
      - key: staffRoleId
        scope: run
      - key: highStaffRoleId
        scope: run
